A knowledge graph is a knowledge base that represents data as a graph of entities and the relationships between them. Knowledge graphs are often stored using semantic web standards such as RDF, which models data as subject-predicate-object triples. Well-known examples of knowledge graphs include DBpedia and Wikidata. Because a knowledge graph makes entities and relationships explicit, it supports structured querying and traversal of connected facts.
